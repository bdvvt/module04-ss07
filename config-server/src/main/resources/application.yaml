spring:
  application:
    name: config-server
  profiles:
    active: native


  cloud:
    config:
      server:
        git:
          uri: https://github.com/bdvvt/module04-ss07.git
          default-label: main
          search-paths: config-server/src/main/resources/config-repo
          clone-on-start: true

server:
  port: 8888
